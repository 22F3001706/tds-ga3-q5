name: CI  # Name your workflow "CI"

on: [push, pull_request]  # Runs when you push or make a pull request

jobs:
  test:
    runs-on: ubuntu-latest  # Uses a free Linux machine
    steps:
    - uses: actions/checkout@v4  # Grabs your code
    - name: Run a test  # Does a simple check
      run: echo "Hello, CI works!"  # Replace with real tests later
